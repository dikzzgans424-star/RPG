[build]
  publish = "public"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Map /api/xxx -> Netlify Function "xxx", supaya frontend (app.js) tidak perlu diubah
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
